#include <stdio.h>

int main()
{
    int x;
    double y;

    printf("Enter x:");
    scanf("%d", &x);

    if(x != 0)
        y = 1.0/x;
    else
        y = 0;

    printf(" y = f(%d) = %.2f", x, y);

    return 0;
}
