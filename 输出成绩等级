#include <stdio.h>

int main()
{
    int grade;
    scanf("%d", &grade);

    if(grade >= 90)
        printf("A");
    if(grade >= 80 && grade <= 89)
        printf("B");
    if(grade >= 70 && grade <= 79)
        printf("C");
    if(grade >= 60 && grade <= 69)
        printf("D");
    if(grade <60)
        printf("E");

    return 0;
}
